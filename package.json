{
  "name": "one-bullet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One bullet. Every shot matters. Browser arcade shooter with authoritative multiplayer.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server.js && node --check public/engine.js && node --check public/app.js && node --check public/render.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
